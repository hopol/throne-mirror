name: 同步 Throne 上游源码

on:
  schedule:
    # UTC 02:00；每月 1、6、11、16、21、26、31 日运行。
    - cron: '0 2 */5 * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: throne-source-sync
  cancel-in-progress: false

env:
  UPSTREAM_REPO: throneproj/Throne
  UPSTREAM_BRANCH: dev
  MIRROR_REPO: https://github.com/hopol/throne-mirror

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 检出镜像仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 获取上游提交
        id: upstream
        shell: bash
        run: |
          set -euo pipefail
          git remote add upstream "https://github.com/${UPSTREAM_REPO}.git" 2>/dev/null || true
          git fetch upstream "${UPSTREAM_BRANCH}" --tags
          echo "commit=$(git rev-parse "upstream/${UPSTREAM_BRANCH}")" >> "$GITHUB_OUTPUT"

      - name: 判断上游是否变化
        id: changes
        shell: bash
        run: |
          set -euo pipefail
          old_commit="$(git show HEAD:upstream/.sync-info 2>/dev/null | sed -n 's/^commit=//p' | head -n 1 || true)"
          if [[ "${old_commit}" == "${{ steps.upstream.outputs.commit }}" ]]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "上游没有新提交。"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: 导出上游源码
        if: steps.changes.outputs.changed == 'true'
        shell: bash
        run: |
          set -euo pipefail
          rm -rf upstream
          mkdir -p upstream
          git archive --format=tar "upstream/${UPSTREAM_BRANCH}" | tar -x -C upstream

          version="$(sed -n 's/^project *( *Throne *VERSION *\([^ )]*\).*/\1/p' upstream/CMakeLists.txt | head -n 1 || true)"
          version="${version:-unknown}"
          commit="${{ steps.upstream.outputs.commit }}"
          cat > upstream/.sync-info <<EOF
          commit=${commit}
          timestamp=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          upstream_url=https://github.com/${UPSTREAM_REPO}
          upstream_branch=${UPSTREAM_BRANCH}
          version=${version}
          EOF
          echo "version=$version" >> "$GITHUB_ENV"
          echo "short_commit=${commit:0:7}" >> "$GITHUB_ENV"

      - name: 提交源码镜像并创建标签
        if: steps.changes.outputs.changed == 'true'
        shell: bash
        run: |
          set -euo pipefail
          tag="mirror-source-v${version}-${short_commit}"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add upstream
          git commit -m "chore: mirror Throne source ${version} (${short_commit})"
          git tag -a "$tag" -m "Mirror Throne source v${version} (${short_commit})"
          git push origin HEAD
          git push origin "$tag"
